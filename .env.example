# Render 部署時需要設定的環境變數
# 在 Render Dashboard -> Environment -> Environment Variables 中設定

# MongoDB Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourAppName
MONGODB_DATABASE=mongodb

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here-at-least-256-bits-long
JWT_EXPIRATION=86400000

# Server Port (Render 會自動設定)
PORT=8080

# CORS Configuration (前端網址)
CORS_ALLOWED_ORIGINS=https://se-project2025-frontend-8qpw.vercel.app

# Logging Configuration (生產環境建議使用 INFO)
SECURITY_DEBUG=false
LOG_LEVEL_SECURITY=INFO
LOG_LEVEL_WEB=INFO

